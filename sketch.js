let canvasW=600,canvasH=400;let paddleW=10,paddleH=80;let playerX=10,playerY=canvasH/2-paddleH/2;let cpuX=canvasW-paddleW-10,cpuY=canvasH/2-paddleH/2;let playerSpeed=6,cpuMaxSpeed=5;let ballR=8,ballX=canvasW/2,ballY=canvasH/2,ballVX=4,ballVY=3;let leftScore=0,rightScore=0;let cpuMissCounter=0,cpuMissTargetY=cpuY;let cpuMissProb=0.01,cpuMissDuration=30;function resetBall(direction){ballX=canvasW/2;ballY=canvasH/2;let dir=0;if(typeof direction==='number'){dir=direction;}if(dir===0){ballVX=4*(Math.random()<0.5?1:-1);}else{ballVX=4*(dir>0?1:-1);}ballVY=3*(Math.random()<0.5?1:-1);}function setup(){createCanvas(canvasW,canvasH);resetBall(Math.random()<0.5?1:-1);textSize(32);textAlign(CENTER,TOP);}function draw(){background(0);fill(255);rect(playerX,playerY,paddleW,paddleH);rect(cpuX,cpuY,paddleW,paddleH);ellipse(ballX,ballY,ballR*2,ballR*2);if(keyIsDown(UP_ARROW)){playerY-=playerSpeed;}if(keyIsDown(DOWN_ARROW)){playerY+=playerSpeed;}playerY=constrain(playerY,0,canvasH-paddleH);if(cpuMissCounter<=0){if(Math.random()<cpuMissProb){cpuMissCounter=cpuMissDuration;cpuMissTargetY=Math.max(0,Math.min(canvasH-paddleH,Math.random()*(canvasH-paddleH)));}}let targetY=cpuMissCounter>0?cpuMissTargetY:(ballY-paddleH/2);let dy=targetY-cpuY;let move=0;if(dy>0){move=Math.min(dy,cpuMaxSpeed);}else{move=Math.max(dy,-cpuMaxSpeed);}cpuY+=move;cpuY=constrain(cpuY,0,canvasH-paddleH);if(cpuMissCounter>0){cpuMissCounter--;}ballX+=ballVX;ballY+=ballVY;if(ballY-ballR<=0){ballY=ballR;ballVY=-ballVY;}if(ballY+ballR>=canvasH){ballY=canvasH-ballR;ballVY=-ballVY;}if(ballVX<0){if(ballX-ballR<=playerX+paddleW&&ballX-ballR>=playerX){if(ballY>=playerY-ballR&&ballY<=playerY+paddleH+ballR){ballX=playerX+paddleW+ballR;ballVX=-ballVX;let diff=ballY-(playerY+paddleH/2);let norm=diff/(paddleH/2);ballVY=norm*5;}}}else{if(ballX+ballR>=cpuX&&ballX+ballR<=cpuX+paddleW){if(ballY>=cpuY-ballR&&ballY<=cpuY+paddleH+ballR){ballX=cpuX-ballR;ballVX=-ballVX;let diff2=ballY-(cpuY+paddleH/2);let norm2=diff2/(paddleH/2);ballVY=norm2*5;}}}if(ballX+ballR<0){rightScore++;resetBall(1);}if(ballX-ballR>canvasW){leftScore++;resetBall(-1);}fill(255);text(leftScore+' : '+rightScore,canvasW/2,10);}
